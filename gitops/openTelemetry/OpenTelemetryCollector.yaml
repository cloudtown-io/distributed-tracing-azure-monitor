apiVersion: opentelemetry.io/v1alpha1
kind: OpenTelemetryCollector
metadata:
  name: application-insights
  namespace: distributed-tracing-azure-monitor
spec:
  replicas: 3
  observability: 
    metrics:
      enableMetrics: true
  env:
    - name: INSTRUMENTATIONKEY
      valueFrom: 
        secretKeyRef:
          name: telemetry
          key: InstrumentationKey
  config: |
    receivers:
      otlp:
        protocols:
          grpc:
          http:
    processors:
      memory_limiter:
        check_interval: 1s
        limit_percentage: 75
        spike_limit_percentage: 15
      batch:
        send_batch_size: 10000
        timeout: 10s

    exporters:
      azuremonitor:
        instrumentation_key: b0cbaac7-80b6-4762-b753-3f5d85b30178
      debug:

    service:
      pipelines:
        traces:
          receivers: [otlp]
          processors: [batch]
          exporters: [azuremonitor, debug]
        metrics:
          receivers: [otlp]
          processors: [batch]
          exporters: [azuremonitor, debug]